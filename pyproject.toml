[tool.poetry]
name = "utm-converter-streamlit"
version = "0.1.0"
description = "A Streamlit application to convert geographic coordinates to UTM."
authors = ["Your Name <youremail@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"
streamlit = "^1.0"
pyproj = "^3.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"